#include <iostream>
#include <list>

int main() {
    std::list<int> numbers;
    for (int i = 1; i <= 10; ++i) {
        numbers.push_back(i);
    }

    for (int num : numbers) {
        std::cout << num << " ";
    }
    return 0;
}


